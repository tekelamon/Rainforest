// a place to display all products returned from api in card elements
function Products() {
    // get products from api
    // map to card elements
    // return jsx with cards filled in
    return (
        <p>Test</p>
    );
}

export default Products;
